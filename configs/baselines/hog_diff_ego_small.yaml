# HOG-Diff baseline on GraphER's frozen Ego-small split.
hog_diff:
  source_env: HOGDIFF
  python_env: HOGDIFF_PYTHON
  upstream_config: ego.yaml
  native_dataset: ego_small
  num_workers: 0
  generation_batch_size: 40
  training_estimates:
    enabled: false
  runtime:
    device: auto
    cuda_visible_devices: "0"
    timeout_seconds: null
    progress:
      enabled: true
      stream_output: true
      interval_seconds: 15
      iteration_interval: 100
      generation_batch_interval: 1
