diff --git a/src/grapher/models/dhvae_hh/training.py b/src/grapher/models/dhvae_hh/training.py
index 1e22969..df6beaa 100644
--- a/src/grapher/models/dhvae_hh/training.py
+++ b/src/grapher/models/dhvae_hh/training.py
@@ -137,10 +137,6 @@ def _train_typed_signature_vae(
                 logvar,
                 beta=effective_beta,
                 weights=weights,
-                prior_outputs=prior_outputs,
-                prior_distribution_sigma=float(
-                    degree_cfg.get("prior_distribution_kernel_sigma", 0.25)
-                ),
             )
             optimizer.zero_grad(set_to_none=True)
             loss.backward()
@@ -386,6 +382,10 @@ def main() -> None:
                 logvar,
                 beta=effective_beta,
                 weights=weights,
+                prior_outputs=prior_outputs,
+                prior_distribution_sigma=float(
+                    degree_cfg.get("prior_distribution_kernel_sigma", 0.25)
+                ),
             )
             optimizer.zero_grad(set_to_none=True)
             loss.backward()
diff --git a/tests/test_typed_invariant_prior.py b/tests/test_typed_invariant_prior.py
index 4ac4cf0..01515cf 100644
--- a/tests/test_typed_invariant_prior.py
+++ b/tests/test_typed_invariant_prior.py
@@ -19,6 +19,7 @@ from grapher.models.dhvae_hh.typed_degree_vae import (
     load_typed_signature_checkpoint,
     save_typed_signature_checkpoint,
 )
+from grapher.models.dhvae_hh.training import _train_typed_signature_vae
 
 
 def _typed_cycle(atom_types: tuple[int, ...] = (6, 6, 8, 8)) -> nx.Graph:
@@ -110,3 +111,49 @@ def test_typed_vae_checkpoint_and_feasible_decode(tmp_path) -> None:
             )
             == []
         )
+
+
+def test_typed_training_smoke_writes_checkpoint(tmp_path) -> None:
+    graphs = [_typed_cycle(), _typed_cycle((6, 7, 8, 6))]
+    checkpoint = tmp_path / "typed" / "checkpoint.pt"
+    config = {
+        "typed_signature": {
+            "node_attribute": "atomic_num",
+            "edge_attribute": "bond_type",
+            "edge_categories": [1, 2, 3],
+            "max_ordinary_degree": 4,
+            "max_weighted_valence": {6: 4.0, 7: 4.0, 8: 3.0},
+        },
+        "constructor": {"ensure_connected": True},
+    }
+    degree_cfg = {
+        "epochs": 1,
+        "batch_size": 2,
+        "latent_dim": 4,
+        "hidden_dim": 8,
+        "size_condition_dim": 4,
+        "prior_type": "conditional_gmm",
+        "prior_components": 2,
+        "num_layers": 1,
+        "dropout": 0.0,
+        "learning_rate": 1.0e-3,
+        "weight_decay": 0.0,
+        "kl_loss_weight": 0.005,
+        "progress_interval": 1,
+    }
+
+    _train_typed_signature_vae(
+        config=config,
+        degree_cfg=degree_cfg,
+        train_graphs=graphs,
+        checkpoint_path=checkpoint,
+        device=torch.device("cpu"),
+        dataset_record={"name": "typed_smoke", "num_train_graphs": len(graphs)},
+    )
+
+    assert checkpoint.exists()
+    metrics_path = checkpoint.parent / "training_metrics.json"
+    assert metrics_path.exists()
+    metrics = json.loads(metrics_path.read_text())
+    assert metrics["final"]["epoch"] == 1.0
+    assert np.isfinite(metrics["final"]["loss"])
